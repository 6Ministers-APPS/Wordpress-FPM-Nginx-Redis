Ниже представлен подробный `README.md` файл, структурирующий и описывающий все предоставленные конфигурационные файлы. Этот документ можно положить в корень репозитория для онбординга новых разработчиков или для собственного справочника.

---

# 🚀 WordPress High-Performance Docker Stack (Coolify Ready)

Этот репозиторий содержит конфигурацию для развертывания высокопроизводительного, защищенного стека WordPress. Сборка оптимизирована для работы в контейнерных средах (например, **Coolify**), использует PHP 8.4 (FPM), Nginx с FastCGI-кэшированием, Redis и тюнинг базы данных MariaDB.

---

## 📂 Структура и описание файлов

### 1. Подготовка сервера (Host Machine)

#### 🛠 `setup.sh`

Скрипт первичной настройки чистого VPS. **Запускается один раз от имени root на хост-машине.**

* **Системные обновления:** Полное обновление пакетов (`apt update && upgrade`).
* **Swap:** Проверяет наличие файла подкачки. Если нет — создает Swap на 2GB (критично для стабильности MySQL/Redis на небольших серверах).
* **Kernel Tuning:** Настраивает `vm.overcommit_memory = 1` (необходимо для корректной работы Redis).
* **Безопасность (SSH & Firewall):**
* Генерирует SSH-ключи (Ed25519) для доступа.
* Настраивает UFW (открывает порты 22, 80, 443, 443/udp).
* **Hardening SSH:** Отключает вход по паролю, разрешает только ключи.
* Устанавливает и настраивает **Fail2Ban** для защиты от брутфорса.


* **Docker:** Устанавливает Docker, если он отсутствует.

## ⚡ Быстрый старт (Настройка сервера)

Для автоматической подготовки чистого сервера (установка Docker, настройка Swap, Firewall, генерация SSH-ключей) выполните одну команду от имени **root**:

```bash
curl -sSL https://raw.githubusercontent.com/SovershenstvoO/Wordpress-FPM-Nginx-Redis/refs/heads/master/setup.sh | bash

```

---

### 2. Оркестрация и Сборка (Docker)

#### 🐳 `docker-compose.yaml`

Основной файл описания сервисов. Определяет стек:

* **wordpress:** Основное приложение (собирается из `Dockerfile`).
* **nginx:** Веб-сервер и реверс-прокси (образ `fholzer/nginx-brotli` с поддержкой сжатия Brotli).
* **mariadb:** База данных (MariaDB 11).
* **redis:** Объектный кэш и хранение сессий.
* **phpmyadmin:** Веб-интерфейс для управления БД (доступен на порту 8070).
* **wp-cron:** Sidecar-контейнер для выполнения задач WordPress по расписанию (linux cron).
* **Healthchecks:** Настроены проверки здоровья для всех контейнеров для автоматического перезапуска при сбоях.

#### 🏗 `Dockerfile`

Инструкция для сборки кастомного образа WordPress.

* **Base Image:** `wordpress:php8.4-fpm` (новейшая версия PHP).
* **Зависимости:** Устанавливает системные либы (`ffmpeg`, `libzip`, `libpng` и др.).
* **PHP Extensions:** Компилирует и включает `pdo_mysql`, `zip`, `bz2`.
* **Redis:** Устанавливает расширение Redis через PECL.
* **WP-CLI:** Скачивает и устанавливает утилиту командной строки для управления WP.
* *Важно:* Удаляет лишний мусор (`apt-get clean`) для уменьшения размера образа.

---

### 3. Конфигурация Веб-сервера (Nginx)

#### 🌐 `nginx.conf`

Мощная конфигурация Nginx, заточенная под WordPress.

* **FastCGI Caching:** Настроено серверное кэширование страниц (аналог плагинов кэширования, но быстрее).
* Исключения кэша: POST-запросы, админка, залогиненные пользователи, корзина WooCommerce.


* **Сжатие:** Включены **Gzip** и **Brotli** (уровень 6) для статики и HTML.
* **Image Optimization (CompressX):** Специальные правила `try_files` для отдачи `.avif` и `.webp` версий изображений, если они существуют (поддержка плагина CompressX).
* **Безопасность:**
* Блокировка доступа к скрытым файлам (`.git`, `.env`), логам и конфигам.
* Запрет выполнения PHP в папке `uploads`.
* Блокировка `xmlrpc.php`.
* Заголовки безопасности (XSS, Frame-Options, Permissions-Policy).


* **Таймауты:** Увеличены до 600s для тяжелых операций (импорт/экспорт).

---

### 4. Настройка PHP и WordPress

#### 🐘 `wp-php.ini`

Глобальные настройки PHP.

* **Ресурсы:** `memory_limit = 512M` (для тяжелых плагинов типа Elementor/FluentCRM).
* **Загрузка:** `upload_max_filesize` и `post_max_size` увеличены до 256M.
* **Opcache:** Агрессивные настройки кэширования опкода (`max_accelerated_files = 20000`, `validate_timestamps = 1` для dev-режима). JIT подготовлен, но отключен по умолчанию.
* **Input Vars:** `max_input_vars = 5000` (решает проблемы с сохранением больших меню).

#### ⚡ `php-fpm-tuning.conf`

Настройка пула процессов PHP-FPM.

* **Режим:** `pm = dynamic` (экономит память в простое).
* **Лимиты:** `pm.max_children = 10` (рассчитано под сервер с ~4GB RAM и БД на борту).
* **Anti-leak:** `pm.max_requests = 1000` (перезапуск процессов для предотвращения утечек памяти).

#### 📜 `init-script.sh`

Скрипт автоматизации запуска контейнера WP.

* **Авто-конфиг (`wp-config.php`):**
* Принудительно устанавливает параметры дебага через WP-CLI.
* Фиксит проблему SSL за прокси (Coolify) через `HTTP_X_FORWARDED_PROTO`.


* **MU-Plugins (Must Use):**
* Автоматически создает плагины защиты: блокировка перебора пользователей и скрытие ссылок REST API.


* **Авто-установка:**
* При первом запуске настраивает Redis Object Cache.
* Конфигурирует настройки S3 для плагинов Fluent.
* Генерирует соли безопасности.
* **Скачивает и устанавливает** большой список плагинов (Elementor, Fluent Suite, SEO Press, Redis Cache и др.).


* **Очистка:** Удаляет Hello Dolly, Akismet, `license.txt`, `readme.html`.
* **Права:** Исправляет права доступа (`chown www-data`, `chmod 640 wp-config.php`).

---

### 5. База данных (MariaDB)

#### 🗄 `my.cnf`

Оптимизация производительности MySQL/MariaDB.

* **InnoDB Buffer Pool:** `1G` (Выделяет 1ГБ RAM чисто под кэш данных БД). *Требует корректировки, если на сервере < 4GB RAM.*
* **Logs:** `innodb_flush_log_at_trx_commit = 2` (значительно ускоряет запись ценой теоретической потери данных за 1 сек при крахе ОС).
* **Connections:** Лимит 150 соединений.

#### 🤖 `create-readonly-user.sh`

Скрипт инициализации (запускается при старте контейнера БД).

* Проверяет наличие переменной `READONLY_DB_PASSWORD`.
* Если переменная есть — создает пользователя `monitor_user` с правами только на чтение (`SELECT`). Удобно для подключения внешних систем мониторинга или бэкапов.

---

## 🔧 Переменные окружения (Environment Variables)

Для корректной работы стека в `.env` файле или настройках Coolify должны быть заданы следующие переменные:

| Переменная | Описание |
| --- | --- |
| `SERVICE_FQDN_WORDPRESS` | Полный домен сайта (URL). |
| `SERVICE_USER_WORDPRESS` | Имя пользователя БД. |
| `SERVICE_PASSWORD_WORDPRESS` | Пароль пользователя БД. |
| `SERVICE_PASSWORD_ROOT` | Root пароль для MariaDB. |
| `READONLY_DB_PASSWORD` | (Опционально) Пароль для read-only юзера. |
| `WP_DEBUG` | `true` / `false` (Вкл/выкл дебаг). |

## 🚀 Порядок установки

1. Загрузить файлы на сервер.
2. Запустить `chmod +x setup.sh && ./setup.sh` (Только один раз!).
3. Скопировать выданные SSH ключи.
4. Настроить переменные окружения в `docker-compose.yaml` или панели управления.
5. Запустить стек: `docker-compose up -d --build`.
6. Дождаться завершения работы `init-script.sh` (установки плагинов).